import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata = { title: "Strategy Methodology | Walnut Markets", description: "How Walnut calculates and presents model strategy research." };

export default function StrategyMethodologyPage() {
  return <div className="mx-auto max-w-4xl py-5 sm:py-8"><p className="text-xs font-semibold uppercase tracking-[0.24em] text-emerald-300">Strategies</p><h1 className="mt-2 text-3xl font-semibold text-white">Methodology</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">Walnut strategy records preserve their data sources, rules, execution convention, benchmark, and methodology version so historical research remains auditable.</p><div className="mt-6 space-y-3">{[["Historical and live are distinct", "Historical strategies are backtests. A strategy is marked Live only after Walnut is evaluating it on its stated schedule and persisting prospective model-portfolio changes."],["Entry convention", "Congress and insider research uses public disclosure availability and enters on the next available trading day. Transaction-date simulations are not presented as actionable performance."],["Performance", "Returns and benchmark comparisons use the strategy run's preserved methodology. Cumulative return, CAGR, drawdown, volatility, and related metrics are stored instead of recalculated during page views."],["Data coverage", "Institutional 13F history is still being backfilled. Strategies dependent on incomplete coverage remain out of public rankings until their history is sufficient and clearly documented."],["Limitations", "Model strategy research is informational only, is not investment advice, and cannot guarantee future performance. Historical results do not represent brokerage execution or individual investor outcomes."]].map(([title, text]) => <section key={title} className="rounded-lg border border-white/10 bg-slate-950/35 p-5"><h2 className="font-semibold text-white">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{text}</p></section>)}</div><Link href="/strategies" className="mt-6 inline-flex text-sm font-semibold text-emerald-300 hover:text-emerald-200">Back to strategies</Link></div>;
}
